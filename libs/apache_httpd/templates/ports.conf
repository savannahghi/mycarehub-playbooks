#
# This tells the server to accept incoming requests on the specified port
# or address-and-port combination. If only a port number is specified, the 
# server listens to the given port on all interfaces. If an IP address is 
# given as well as a port, the server will listen on the given port and 
# interface.
#
Listen {{ apache_httpd_listen_address }}
