---

- name: Restart Teleport
  ansible.builtin.systemd_service:
    daemon_reload: yes
    enabled: yes
    name: teleport
    scope: system
    state: restarted
  become: yes
  tags: ["teleport_ssh_service"]
